# 静态资源（sprite/bg 雪碧图、JS、CSS）由 app.js / index.html 统一加 ?v= 版本戳。
# 资源改了 → 戳变 → URL 变 → 浏览器/CF 必然拉新；戳不变则长缓存，但不用 immutable
# 避免偶发坏响应（HTML fallback / 500）被锁死一年。
/talent-sim/assets/*
  Cache-Control: public, max-age=86400

# HTML 永远取最新（页面结构 / 逻辑更新即时生效，不被 CF 缓存成旧壳）
/*.html
  Cache-Control: no-cache

# 禁止 CF 对图片做 Polish/转码（webp 被转码/压缩会破坏雪碧图定位）
/talent-sim/assets/sprites/*
  Cache-Control: public, max-age=86400, no-transform
