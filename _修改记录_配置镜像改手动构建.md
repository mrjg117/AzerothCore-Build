# 修改记录 · 配置镜像改为仅手动构建

- 日期：2026-08-13
- 涉及文件：
  - `.github/workflows/build-config.yml`（实际改动）
  - `README.md`（文档同步）
- 触发人：用户（"把配置镜像 改成手动构建吧"）

## 一、背景与正确参考点判定

`ac-extra-config` 镜像的**实际构建位置**是独立 workflow `build-config.yml`，**不在** `build-core.yml`（后者只构筑 worldserver/authserver/db-import/tools 四个官方 target）。

> ⚠️ 文档/记忆不一致说明：早期项目记忆曾描述"三镜像族随核心自动推 ghcr.io"，把 `ac-extra-config` 当成 `build-core` 自动推送的一部分。**该描述与当前代码不符**——以 `.github/workflows/` 实际内容为唯一正确参考点：`ac-extra-config` 一直由 `build-config.yml` 单独构建。

## 二、改动前状态（build-config.yml 的 `on:`）

```yaml
on:
  workflow_dispatch:
  push:
    branches: [main, master]
    paths:
      - "config/**"
```

即：既支持**手动** `workflow_dispatch`，又在**每次推送触及 `config/**` 时自动重建**配置镜像。

## 三、改动后状态

```yaml
on:
  workflow_dispatch:
```

删除 `push:` 自动触发块，仅保留手动 `workflow_dispatch`。

## 四、改动原因（用户意图）

- 改 `config/modules.txt`、改 `config/extra-config/confs/*` 等都会触动 `config/**`，此前会**自动触发一次配置镜像重建**——属于无谓的 CI 开销与不可控的自动发布。
- 用户要求配置镜像改为**显式手动构建**：只有主动在 Actions 页面点「Run workflow」才构建并推送 `ac-extra-config`。

## 五、影响与边界

- 不影响 `build-core.yml`（核心编译）与 `build-maps.yml`（地图）的触发方式。
- 改动 `config/extra-config/confs/*` 后，**必须手动跑一次 `build-config.yml`** 服务端才会重新注入新配置——README「维护」段已同步说明。
- 推送本修改（仅触及 `.github/workflows/`，不触及 `config/**`）**不会**触发任何 workflow 自动运行（build-config 的 push 触发器已删、build-core 无 push 触发器）。

## 六、附带一致性修正（README）

- README 中"42 模组"统一改为"40 模组"（modules.txt 经多轮剔除现为 40 个，原文档数字已过时）。
- README 架构表 `ac-extra-config` 行注明"仅手动 workflow_dispatch"。
- README「CI 工作流」与「维护」段同步描述配置镜像为手动构建。

## 七、验证

- 改动仅删 YAML 触发块，不改变 `docker buildx build config/extra-config` 的构建逻辑。
- 提交后需在 Actions 页面 `build-config` 确认：手动 `Run workflow` 仍可成功构建并推送 `ac-extra-config:latest`；且此后向 `main` 推送 `config/**` 变更**不再**自动触发该 workflow。
