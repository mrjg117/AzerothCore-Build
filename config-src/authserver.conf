# ============================================================
# authserver.conf —— 部分覆盖
# ------------------------------------------------------------
# 只写必须改的键；其余用 AC 内置默认值。
# ============================================================

# 登录库指向 docker-compose 的 db 服务
LoginDatabaseInfo = "db;3306;root;acore;acore_auth"
