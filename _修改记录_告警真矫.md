# 修改记录 · 构建告警「真矫」而非「屏蔽」

- 日期：2026-08-13
- 关联提交：`b187d54..4d285f4` 期间的告警处理被推翻重做；本记录对应最新修正提交。
- 动机：用户要求「能真矫就真矫，不能矫也不用屏蔽」——撤销上一轮 `-Wno-*` 抑制补丁，改为在源码层面对可安全修复的告警做真实修正。

## 一、已撤销（纠正上一轮的错误做法）
- 删除 `build-patches/mod-item-affixes|mod-mythic-plus|mod-playerbots/` 的 `suppress_warnings.sh`（`-Wno-unused-parameter` 等全局抑制 = 掩盖，非矫正）。

## 二、新增 build-patches（全部为源码层真矫，走统一机制）

| 模组 | 文件 | 修正内容 | 类型 |
|---|---|---|---|
| `mod-junk-to-gold-plus` | `mod_junk_to_gold_plus.cpp:90` | `AllowableClass == -1` → `== uint32(-1)`（uint32 与 int 比较，显式转换保语义） | sign-compare |
| `mod-mythic-plus` | `mythic_plus_npc_support.cpp:175` | `for (int i ...)` → `for (size_t i ...)`（与 `size()` 返回类型一致） | sign-compare |
| `mod-playerbots` | `Ai/Raid/Mag/MagActions.cpp:295` | `warlockIndex < abyssals.size()` → `< static_cast<int>(abyssals.size())`（保留 int 语义，零行为变化） | sign-compare |
| `mod-item-affixes` | `ItemAffixScripts.cpp:263/372` | 删除本 TU 内从未引用的 `static constexpr`（`SPELL_CELESTIAL_RESONANCE`/`SPELL_VANISHING_BACKSTAB_ID`，内部链接、确为死代码） | unused-const-variable |

## 三、判定为「不能干净真矫、按规则留着」的告警（不处理）
- **接口强制的 unused parameter（占告警主体，约 80 条中的绝大部分）**：如 `mod-item-affixes` 的 `ImprintMgr.h` 虚函数基类默认实现 `OnEquip(Player*, uint64)` 等空函数体——参数是 AC 脚本 hook 签名的一部分，子类 override 依赖它，**删不掉、也无权改名**；唯一「修法」是 `(void)x` 或 `-Wno-`，本质都是屏蔽 → 留着。
- **头文件级私有成员 / 需改调用点的参数**：如 `mod-playerbots` 的 `MovementActions.h:distance`、`FleeManager.h:botAI`、`RandomPlayerbotMgr.h:processTicks/bgBotsCount`、`MagHelpers.cpp:117 bot` 参数——删除字段需扫全模块确认无引用、删参数要改所有调用方，动它风险大 → 留着。
- **多行死函数**：`mod-item-affixes` `ItemAffix.cpp:716 NeedsMainStatSelector`（无调用者的 static，但跨 7 行，sed 按行号删脆弱，上游不锁 commit）→ 留着标注。
- **官方 Dockerfile 的 apt man-pages 噪声（~18 条）**：非编译器告警，来自官方镜像 `apt-get install`，按铁律不碰官方镜像 → 留着。

## 四、参考点判定
- 以 **run 31662273451 的完整 job 日志（blob 已落盘）** 为唯一真相源，逐条抽出 `文件:行号 + 警告类型`，再做可矫性判定。
- 之前那批 `~83` 编译器告警全部来自 6 个上游模组，均不触发 `-Werror`、不影响链接与运行。

## 五、生效方式
- `build-core` 为手动触发；本提交 push 后**不会**自动重建。需手动 Run workflow `build-core` 才会应用上述补丁并验证告警下降。
