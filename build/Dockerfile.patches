# ============================================================
# 补丁数据镜像（wotlk-patches）—— 与服务器镜像**分体构建**
# ------------------------------------------------------------
# ・本镜像只做静态文件 COPY，不在 CI 内重新生成补丁。
# ・所有产物（MPQ + AddOn）已由本地生成并 vendored 在
#   client-patches/ 中，作为唯一真相源，且**按模组分子目录**存放：
#     client-patches/item-affixes/        （mod-item-affixes：MPQ + AddOn）
#     client-patches/guild-levels/        （mod-guild-levels：仅 AddOn）
#     client-patches/bot-inventory-master/（mod-bot-inventory-master：仅 AddOn）
#   - 每个模组目录下的 mpq/ 与 addon/ 分开，便于定位「哪个修改来自哪个模组」。
# ・下载页主推的整包 patches-client.zip 也在该目录顶层。
# ・生成与格式修正记录见 client-patches/README.md。
# ============================================================

FROM busybox:1.36
WORKDIR /patches

# 整棵 client-patches 树（保留按模组分子目录的布局）原样 COPY 进镜像
COPY client-patches /patches

VOLUME ["/patches"]
