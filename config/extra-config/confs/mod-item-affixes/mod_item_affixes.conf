#############################################
#dont edit this file its for the modules
#############################################
# mod-item-affixes 自定义覆盖（按模组分配置）
# 仅列出与模块默认不同的项；未列出的键自动沿用模块 conf.dist 默认值。
#############################################

# 防御性锁定：WotLK 平衡预算
# 模块 conf.dist 默认 StatMultiplier = 3.0（偏强/爽服向），
# 这里锁回 1.0（WotLK 原版数值精度），避免掉落数值膨胀。
ItemAffixes.StatMultiplier = 1.0
